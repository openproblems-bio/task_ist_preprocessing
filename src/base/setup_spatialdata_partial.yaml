setup:
  - type: python
    pypi: ["spatialdata==0.6.0", "anndata>=0.12.0", "setuptools<70"]
    # setuptools<70 is required because xarray-schema (a spatialdata dependency) imports
    # pkg_resources, which is only exposed as a top-level module in setuptools<70.
