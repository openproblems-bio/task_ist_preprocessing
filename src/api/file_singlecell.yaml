type: file
example: "resources_test/task_ist_preprocessing/2023_yao_mouse_brain_scrnaseq_10xv2/dataset.h5ad"
label: "SC Dataset"
summary: A single-cell reference dataset, preprocessed for this benchmark.
description: |
  This dataset contains preprocessed counts and metadata for single-cell RNA-seq data.
__merge__: file_common_singlecell.yaml
info: #TODO: check if merge with existing layers from file_common_singlecell.yaml works
  format:
    type: h5ad
    layers: 
      - type: integer
        name: raw
        description: Raw counts
        required: true
      - type: integer
        name: norm
        description: Normalised counts
        required: true
      - type: integer
        name: lognorm
        description: Log normalised counts
        required: true