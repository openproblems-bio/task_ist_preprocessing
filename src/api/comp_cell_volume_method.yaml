info:
  type: cell_volume
  type_info:
    label: Cell Volume Calculation
    summary: Calculate the volume of cells
    description: Calculate the volume of cells based on the transcripts that are assigned to them.
arguments:
  - name: "--input"
    __merge__: file_spatialdata_assigned.yaml
    direction: output
    required: true
  - name: "--output"
    __merge__: file_cell_volumes.yaml
    direction: output
    required: true
  # - name: "--input"
  #   __merge__: file_spatialdata_segmented.yaml
  #   direction: input
  #   required: false